'use client';
import {useState} from 'react';
import {sfx} from '@/lib/audio';
export default function Fractions({onAnswer,disabled,sound=false}:{onAnswer:(s:string)=>void;disabled:boolean;sound?:boolean}){const [selected,setSelected]=useState<number[]>([]);return <div className="fraction-lab"><p className="tool-help">Bu bütün dört eş parçaya ayrıldı. İstediğin parçalara dokunarak boya.</p><div className="quarter-circle">{[0,1,2,3].map(i=><button key={i} disabled={disabled} className={`quarter q${i} ${selected.includes(i)?'is-filled':''}`} aria-pressed={selected.includes(i)} aria-label={`${i+1}. çeyrek`} onClick={()=>{const next=selected.includes(i)?selected.filter(x=>x!==i):[...selected,i];setSelected(next);onAnswer(String(next.length));sfx(sound,next.length>selected.length?'place':'remove')}}><span>{selected.includes(i)?'✓':''}</span></button>)}</div><p aria-live="polite">{selected.length} çeyrek seçtin.{selected.length===2?' İki çeyrek bir yarım eder.':selected.length===4?' Dört çeyrek bir bütün eder.':''}</p></div>}
