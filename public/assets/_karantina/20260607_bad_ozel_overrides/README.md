# Bad ozel PNG overrides quarantined

These special-slot PNG layers were removed from the active manifest because they do not match metadata or obscure the model. The `ozel` vector list is currently empty, so the tab remains inactive until replacement assets are generated.

- `oz_bodysuit`: reads as partial lower garment, not a clean full bodysuit layer.
- `oz_catears`: face-obscuring mask/large object, not small cat ears.
- `oz_harness`: acceptable direction but coupled to the bad special set; regenerate with the same prompt contract.
- `oz_latex`: red garment, conflicts with metadata/name `Siyah Lateks`.
