# Bad dress PNG overrides quarantined

These PNG dress layers were removed from active manifest because they include baked shoes/skin or do not match their prompt. The vector wardrobe fallback remains available in `public/dolap.js`.

- `elb_esofman`: PNG contains baked shoes and conflicts with shoe slot; vector fallback is cleaner.
- `elb_kazak`: PNG does not match cozy sweater dress brief and reads as lingerie/skin overlay; vector fallback is cleaner.
- `elb_kot_ceket`: PNG contains baked shoes/full outfit artifacts and conflicts with shoe slot; vector fallback is cleaner.
- `elb_okul`: PNG contains socks/shoes baked into dress layer and conflicts with shoe slot; vector fallback is cleaner.
- `elb_spor_lila`: PNG contains baked shoes/full outfit artifacts and conflicts with shoe slot; vector fallback is cleaner.
- `elb_kis_mavi`: PNG does not match winter dress brief and reads as swimsuit/skin overlay; vector fallback added in `public/dolap.js`.
- `elb_deniz_turkuaz`: PNG does not match turquoise summer dress brief and reads as black swimsuit/sheer pareo; vector fallback added in `public/dolap.js`.
